
const initialState = {
    checking: true,
    // uid: null,
    // name: null
}

const authReducer = ( state = initialState, action ) => {

    switch ( action.type ) {
    
        default:
            return state;
    }
    
};

export default authReducer;